import { CustomTitleStrategy } from './custom-title-strategy.strategy';

describe('CustomTitleStrategy', () => {
  it('should create an instance', () => {
    expect(new CustomTitleStrategy()).toBeTruthy();
  });
});
