import { HybridPreloadingStrategy } from './hybrid-preloading.strategy';

describe('HybridPreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new HybridPreloadingStrategy()).toBeTruthy();
  });
});
